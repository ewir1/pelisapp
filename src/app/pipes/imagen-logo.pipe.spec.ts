import { ImagenLogoPipe } from './imagen-logo.pipe';

describe('ImagenLogoPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenLogoPipe();
    expect(pipe).toBeTruthy();
  });
});
